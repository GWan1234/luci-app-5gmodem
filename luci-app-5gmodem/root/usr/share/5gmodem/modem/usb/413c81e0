# Dell DW5821e-eSIM (413c:81e0) - тот же модуль T77W968, что 413c:81d7 и
# 0489:e0b5: прошивка одна (T77W968.F1.0.0.5.2.GC.013), AT-набор один. Файл был
# побайтовой копией 81d7 с точечными «tr -d \r» и успел разойтись: лексическая
# сортировка температуры, RSSI из «rx_diversity:», hex-TAC - всё, что в 81d7
# уже починили, здесь жило дальше (ревью 12.09.2026). Переиспользуем дельный
# профиль: CR он теперь режет у источника, ID берёт у устройства.
. "$RES/modem/usb/413c81d7"
