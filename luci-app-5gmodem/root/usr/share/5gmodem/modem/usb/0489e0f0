# Foxconn T99W373 (Thales Cinterion MV32-W, Qualcomm SDX62) - композиция MBIM.
#
# Идентификатор из таблицы ядра (drivers/usb/serial/option.c: «Foxconn T99W373
# MBIM», RSVD(3)) - интерфейс 3 отдан каналу данных, AT приходит на один из
# оставшихся ttyUSB, привязку делает сам option без нашего new_id.
#
# Логика метрик - в общем файле: тот же модуль ходит и по PCIe (105b:e0d9), а
# AT-набор у обеих шин ОДИН. У родственного MV31 общего файла не сделали, и
# шесть копий разошлись - правку вносили в одну.
. "$RES/modem/_foxconn_mv32_common"
